'use strict';
//rememeber to add *.js files to index page
registrationModule.factory('accountRepository', ['$resource', function ($resource){
    return{
        save: function (student) {
            return $resource('api/Account').save(student);
        }
    }
}]);